diff --git a/pango/pango-item.c b/pango/pango-item.c
index b6a9c67..15b47fb 100644
--- a/pango/pango-item.c
+++ b/pango/pango-item.c
@@ -101,8 +101,8 @@ pango_item_free (PangoItem *item)
 
   if (item->analysis.extra_attrs)
     {
-      g_slist_foreach (item->analysis.extra_attrs, (GFunc)pango_attribute_destroy, NULL);
-      g_slist_free (item->analysis.extra_attrs);
+      g_slist_free_full (item->analysis.extra_attrs,
+                         (GDestroyNotify)pango_attribute_destroy);
     }
 
   if (item->analysis.lang_engine)
diff --git a/pango/pango-markup.c b/pango/pango-markup.c
index 3b0bc54..99bc10d 100644
--- a/pango/pango-markup.c
+++ b/pango/pango-markup.c
@@ -193,8 +193,8 @@ scale_factor (int scale_level, double base)
 static void
 open_tag_free (OpenTag *ot)
 {
-  g_slist_foreach (ot->attrs, (GFunc) pango_attribute_destroy, NULL);
-  g_slist_free (ot->attrs);
+  g_slist_free_full (ot->attrs,
+                     (GDestroyNotify) pango_attribute_destroy);
   g_slice_free (OpenTag, ot);
 }
 
diff --git a/pango/pango-context.c b/pango/pango-context.c
index 402aac6..d75e403 100644
--- a/pango/pango-context.c
+++ b/pango/pango-context.c
@@ -722,8 +722,7 @@ pango_context_get_metrics (PangoContext               *context,
 
   update_metrics_from_items (metrics, language, sample_str, text_len, items);
 
-  g_list_foreach (items, (GFunc)pango_item_free, NULL);
-  g_list_free (items);
+  g_list_free_full (items, (GDestroyNotify)pango_item_free);
 
   g_object_unref (current_fonts);
 
diff --git a/pango/pangofc-font.c b/pango/pangofc-font.c
index 9eb046d..761a238 100644
--- a/pango/pangofc-font.c
+++ b/pango/pangofc-font.c
@@ -167,8 +167,8 @@ pango_fc_font_finalize (GObject *object)
   PangoFcFontPrivate *priv = fcfont->priv;
   PangoFontMap *fontmap;
 
-  g_slist_foreach (fcfont->metrics_by_lang, (GFunc)free_metrics_info, NULL);
-  g_slist_free (fcfont->metrics_by_lang);
+  g_slist_free_full (fcfont->metrics_by_lang,
+                     (GDestroyNotify)free_metrics_info);
 
   fontmap = fcfont->fontmap;
   if (fontmap)
diff --git a/pango/pangocairo-font.c b/pango/pangocairo-font.c
index bfaf734..121cfb9 100644
--- a/pango/pangocairo-font.c
+++ b/pango/pangocairo-font.c
@@ -1134,8 +1134,8 @@ _pango_cairo_font_private_finalize (PangoCairoFontPrivate *cf_priv)
     g_free (cf_priv->glyph_extents_cache);
   cf_priv->glyph_extents_cache = NULL;
 
-  g_slist_foreach (cf_priv->metrics_by_lang, (GFunc)free_metrics_info, NULL);
-  g_slist_free (cf_priv->metrics_by_lang);
+  g_slist_free_full (cf_priv->metrics_by_lang,
+                     (GDestroyNotify)free_metrics_info);
   cf_priv->metrics_by_lang = NULL;
 }
 
